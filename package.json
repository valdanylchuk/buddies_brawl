{
  "devDependencies": {
    "@types/node": "^24.0.15",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.30.1",
    "globals": "^16.3.0",
    "jsdom": "^26.1.0",
    "tsx": "^4.20.3",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "build": "tsc && copyfiles -u 1 public/* public/**/* dist",
    "tournament": "tsx scripts/tournament.ts",
    "serve": "http-server dist"
  },
  "dependencies": {
    "copyfiles": "^2.4.1"
  }
}
